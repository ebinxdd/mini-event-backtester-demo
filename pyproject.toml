[project]
name = "mini-event-backtester-demo"
version = "0.1.0"
description = "Sanitized event-driven backtester demo with synthetic data."
requires-python = ">=3.10"
dependencies = []

[tool.pytest.ini_options]
pythonpath = ["src"]

